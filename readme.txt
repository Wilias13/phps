before - это исходный код с dvwa
Статический анализатор указал только одну уязвимость а именно то что нет обработки полученных таблиц из бд, имеется в виду если данных нет или запрос составлен неверно ошибка не будет обработана и произойдёт краш
sql инъекцию можно сделать вот здесь
if( isset( $_GET[ 'Submit' ] ) ) {
	// Get input
	$id = $_GET[ 'id' ];

переменная id никак не проверяется и не экранируется



файл aster Исправленная версия

добавлено 
-удаление пробелов
-так как нужно просто поискать по id, выполняется запрос только если переменная id это число

Конкретно для этой формы и этой задачи защита будет работать

но если форма будет более сложной с большим кол-вом полей для обработки то я бы принял следующие меры
- использование PDO: отдельно подготавливается запрос и переменные
- экранирование занков: удаление пробелов кавычек и других служебных знаков
- смена метода на POST